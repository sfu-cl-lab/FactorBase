`RunBB.jar` generates the Bayes Net.   
`BIF_Generator.jar` generates the BIF file based on RunBB result.   
Both jar files use `cfg/subsetctcomputation.cfg` as config file and `RunBB.jar` also need `src/scripts`
